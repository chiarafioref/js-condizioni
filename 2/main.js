console.log("connesso");

let weight = prompt("Quanto pesa la tua valigia?");
const price = 36;

if (weight > 23) {
    console.log("Il prezzo del biglietto è " + (price + 20) + " €")
} else {
    console.log("Il  prezzo del biglietto è " + price + " €")
};